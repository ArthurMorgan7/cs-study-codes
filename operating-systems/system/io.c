#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv){

    /* ---------------------------------- C I/O --------------------------------- */

    FILE *fp = fopen("tmp", "r");

    perror("fopen()");

    exit(0);
    /* --------------------------------- sys I/O -------------------------------- */

}